# Function: getDefaultPuller()

> **getDefaultPuller**(`rep`): [`Puller`](../type-aliases/Puller.md)

This creates a default puller which uses HTTP POST to send the pull request.

## Parameters

### rep

#### auth

`string`

#### pullURL

`string`

## Returns

[`Puller`](../type-aliases/Puller.md)
